function bfs(graph, start_node)
    visited = empty list
    queue = empty list

    append start_node to visited
    append start_node to queue

    while queue is not empty
        target = dequeue first element from queue
        print target

        for each adjacent_node in graph[target]
            if adjacent_node is not in visited
                append adjacent_node to visited
                append adjacent_node to queue
